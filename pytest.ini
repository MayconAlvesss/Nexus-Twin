[pytest]
# NexusTwin test configuration
testpaths = tests
asyncio_mode = auto

# Show verbose output so we know which test is failing
addopts = -v --tb=short

# Ignore the virtual environment and the lab scripts directory
norecursedirs = venv .venv lab docker
